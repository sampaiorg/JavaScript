//EXEMPLO APENAS PARA UTILIZAR O MATCH

const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);

console.log(area);
console.log(area.toFixed(2));

// Apenas para exibir que o Math é um objeto e não uma função;
console.log(Math);

